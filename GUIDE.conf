##⇘⇘ Basic description:
## A comment “#” at the beginning of the line means that this item is not enabled (i.e. restoring the default settings of MPV), delete it to enable it.
## If there is no “=” in some of the enabled parameters, the item is actually “yes”. For example, deband is disabled by default: --deband is actually equivalent to --deband=yes; by analogy --deband=no is equivalent to commenting out the parameter.
## Explanation of comments -- # <Optional> [Conditional Requirement] Explanation of the meaning of the parameter (Supplementary)

########
# BASE #
########

## DECODE
vo=gpu-next                           # <gpu/gpu-next/libmpv> Video output driver. Many of the subsequent options will only work properly with this triple. The current version defaults to gpu
                                      ## gpu has the highest pervasiveness and completeness; gpu-next see discussion here https://github.com/hooke007/MPV_lazy/discussions/39; libmpv is designed for some front-ends and is generally not used
                                      ## gpu-next is more promising, currently mapping Dolby Vision RPU layer metadata parsing, not yet supporting EL layer metadata parsing
                                      ## The difference between gpu-next and gpu is described in the official wiki: https://github.com/mpv-player/mpv/wiki/GPU-Next-vs-GPU
gpu-api=d3d11                         # <opengl/vulkan/d3d11> [d3d11 is recommended for SVP framing] Select graphics drawing interface. windows native rendering is d3d11 (mpv is default for windows, note that the graphics card is in adaptive power mode)
                                      ## opengl is the least recommended choice, vulkan's 10bit is not yet fully realized https://github.com/mpv-player/mpv/issues/8554
                                      ## With d3d11-adapter you can set which graphics card mpv uses. See the documentation at https://mpv.io/manual/master/#options-d3d11-adapt
                                      ### Options for gpu-api?
                                      ## Linux/macOS: vulkan or opengl, recommend vulkan
                                      ## Windows: vulkan, d3d11 and opengl are all available. The d3d11 native api is recommended for best performance and 10bit output.
#gpu-context=auto # <win/dxinterop/angle/d3d11/winvk/macvk> Select output backend. This is usually determined automatically by --gpu-api=<value>; angle is the default for --gpu-api=opengl, which is better changed to win/dxinterop
#fbo-format=auto # Internal processing precision. This is usually determined automatically by --gpu-api=<value>, which prefers 16-bit and higher precision by default. This parameter is not modifiable under --vo=gpu-next.
#d3d11-exclusive-fs=yes # --gpu-api=fullscreen-exclusive under d3d11.
#d3d11-flip=no # [usually occurs when --d3d11-exclusive-fs=yes and --on-top are used together] can be used to avoid freezing when MPVs go fullscreen.
                                      # Enable this parameter if the above problem exists, otherwise it should not be used as flip models perform best under d3d11. We have tested that there is a performance limitation in flip mode under N-card driver, so it is recommended to disable it when encountering rendering bottlenecks.
                                      # [When --gpu-context=angle also has fullscreen freeze issue] Use --angle-flip=no instead of d3d11-flip=no.
#d3d11va-zero-copy=yes # [When gpu-api=d3d11] By default, video images are copied (GPU to GPU) from the decoder surface to shader resources when using hardware decoding with --gpu-api=d3d11.
                                      # Set this option to avoid copying by sampling directly from the decoder image. This may improve performance and reduce power consumption, but may cause the image to be sampled incorrectly at the bottom and right edges due to padding, and may trigger driver errors since Direct3D 11 technically does not allow sampling from the decoder surface (although most drivers support it.)               
                                      # Enable this to try to improve when the decoder is stuttering and dropping frames.
hwdec=d3d12va-copy,auto-copy          # Specify the hardware video decoding API that should be used, change to no for softcopy, d3d11va or d3d12va-copy is recommended for hardcopy on Windows. d3d11va has the best hardware decoding support, providing 4k60 fps and native hardcopy at 8k in av1. nvdec supports yuv444 but this format is less common and is not stressed under softcopy. Acceleration. nvdec supports hard decoding of yuv444, but this format is rare and is not stressed under soft decoding
                                      # --hwdec=auto-safe is equivalent to --hwdec=yes. auto prefers d3d11va for --gpu-api=d3d11 and nvdec for --gpu-api=vulkan. auto-copy prefers d3d11va-copy.
                                      # Native hard decoding with no-copy, which is theoretically the most efficient decoding method, is not recommended in order to ensure that all settings/filters/shaders work correctly (some are not supported). Note that no-copy nvdec does not support d3d11, only vulkan and opengl.
                                      ## You can also use a prioritized list of values, e.g. vulkan-copy,nvdec-copy,auto to try these modes in order.
                                      ## It is recommended to use the auto-safe parameter when using auto without specifying a specific hardcoding api, this will prioritize the hardcoding api that is actively supported by the developer (d3d11va or nvdec on windows).
                                      ## Some problems may be encountered in hard mode, it is recommended to prioritize soft mode (the official manual does not recommend hard mode).
                                      ## Tested and found that copy mode under --vo=gpu will drop frames abnormally when the frame rate of 4K and above video is >=60fps, recommend to use soft mode or no copy mode.
#hwdec-codecs=all                     # Attempt to hard-decode in the selected range of codecs, current version defaults to --hwdec-codecs=“h264,vc1,hevc,vp8,vp9,av1,prores”.
#vd-lavc-dr=no                        # [when gpu-api=d3d11] Enable direct rendering (default: auto). If set to yes, video will be decoded directly to GPU video memory (or staging buffer)
                                      # If set to no, video will be decoded to system memory and then copied to GPU memory. This may cause a performance drop, but may solve a problem on some low-end intel GPUs!
                                      # The default value auto is automatically selected based on the capabilities of the decoder and GPU (i.e. automatically disabled for low-end intel GPU devices)
#vd-queue-enable=yes                  # Enable the video decoder queue (default: no). If set to yes, the video decoder will run on a separate thread, which may lead to performance degradation, but may solve the problem on some low-end intel GPUs
#vd-lavc-threads=8                    # Number of threads used for decoding. Whether or not threads are actually supported depends on the codec (default: 0). 0 means that the number of cores on the machine is automatically detected and used, up to a maximum of 16. You can set more than 16 threads manually
#vd-lavc-assume-old-x264=yes          # Assume that the video is encoded from an old, defective version of x264 (default: no). Try to enable this only if video in x264 yuv444p10 format is decoded incorrectly to resolve ffmpeg compatibility issues with older x264 encodings
#opengl-pbo=yes                       # Enable PBO. On some drivers this may be faster, especially if the source video size is large (e.g. so-called “4K” video). On other drivers, it may be slower or cause latency issues!
##⇘⇘⇘⇘⇘⇘⇘⇘⇘⇘⇘⇘ Handling of Dual Graphics Laptops
#vd=h264_qsv,hevc_qsv,mpeg2_qsv,vp9_qsv,vp8_qsv,av1_qsv
                                      # Under the combination of Intel Nucleus & NVDIA_MX_x50, if you use Solo to render, since MX series Solo does not have a decoding unit, even if you use hwdec to specify hard decode, you can still only use soft decode.
                                      # Use this parameter to use core hard decode, auxiliary rendering at the same time let the solo display main rendering (when hwdec=no fast forward may cause MPV false death, it is recommended to use xxxx-copy decode method)
#d3d11-adapter="NVIDIA GeForce RTX 2070 Super”
                                      # [when gpu-api=d3d11] Specify a graphics card as the main rendering output, by default the system-specified main rendering graphics card is automatically selected.
                                      # The graphics card name can be changed by checking the task manager. This parameter is equivalent to setting the driver panel to run mpv with a dedicated graphics card.
                                      # For example, if a dual graphics laptop with I+N is using “Intel(R) UHD Graphics” for the internal screen, changing this parameter to specify a dedicated graphics card reduces the 3d rendering percentage of the dual graphics card in the task manager.
                                      # Most dual graphics laptops behave like a “direct-connect” when they have an external monitor, and will automatically output with a dedicated graphics card without this parameter.
                                      # [When gpu-api=vulkan] use --vulkan-device instead of --d3d11-adapter.
#vulkan-device="NVIDIA GeForce RTX 2070 Super”
                                      # [when gpu-api=vulkan] Instead of --d3d11-adapter, use --vulkan-device=“NVIDIA GeForce RTX 2070 Super”.
                                      # Empty by default, example value “NVIDIA GeForce RTX 2070 Super” (full device name or UUID must be used)

########
# Function #
########

title=${?pause==yes:⏸}${?mute==yes:🔇}${?ontop==yes:📌}${?demuxer-via-network==yes:${media-title}}${?demuxer-via-network==no:${filename}}
                                      # The content of the title displayed on the customization window, which also affects the title attribute of some scripts. Default: ${?media-title:${media-title}}${!media-title:No file}
osc=no                                # [Pre-condition for third-party osc scripts] Disable the original OSC (i.e. the built-in “osc.lua” script) for compatibility with third-party interface styles.
#fs                                   # fs=fullscreen to turn on fullscreen.
#no-border                            # defaults to the system's native window interface, enable this to use the borderless interface (removes Windows' window decorations)
no-title-bar                          # Control whether to use window title bar to play, enabled by default. Unlike --no-border, --no-title-bar preserves Windows' native windowing features
#media-controls=yes                   # <yes|default player|no> Enable integration of the media control interface SMTC (Windows only). 
                                      ## Setting it to player will only use the controls for mpv players; setting it to yes will also enable the libmpv integrated controls.
#backdrop-type=auto                   # <auto|none|mica|acrylic|mica-alt> control background/border style (Windows only)
window-corners=roundsmall             # <default|donotround|round|roundsmall> Preferences for controlling window rounding. default: let system decide whether to round corners or not; donotround: disable rounding; round: enable rounding; roundsmall: enable smaller radius rounding. Enable rounded corners with smaller radius
#window-maximized=yes                 # Run MPV with automatic window maximization (maximization in borderless interface is similar to “borderless window mode” instead of “fullscreen”)
#background=none                      # <none|color|default tiles> If the frame has an alpha channel, specify which background method (if any) to use to blend with it. If it doesn't have an alpha channel, nothing is done
                                      ## none: don't blend the frame and leave the alpha as is; color: blend the frame with the background color (specified by --background-color, defaults to black); tiles: blend the frame with a 16x16 gray/white tiled background
#background-color=“#778899”           # <format (AA)RRGGBB> color used to draw the part of the mpv window not covered by the video
#hidpi-window-scale=yes               # <yes|default no> Whether to enforce HIDPI scaling. This parameter may affect the window size, disable it to keep the window size DPI independent.
                                      # By default the initialized video window changes size with DPI, e.g. automatically performs 2x scaling to 1600p after opening an 800p video on a 4k monitor scaled by 200%, also affects other script sections
#window-scale=1.0                     # Specify how much the window is scaled relative to the original size of the video, default is 1.0.
                                      # Only make the window scale to a true 1 if hidpi-window-scale=no (otherwise it will be measured against the system DPI). Lower priority than geometry and --autofit parameters.
#current-window-scale=1.0             # Same as window-scale, but this option can dynamically resize the current window at runtime.
#auto-window-resize=yes               # <default yes|no> Whether to allow mpv to automatically resize the window when switching between playback files. This option does not affect geometry and autofit.
#geometry=75%x70%                     # <[W[xH]][+-x+-y][/WS]>, <x:y> Specific window size at startup (with the ability to freeze the window size at this point) and/or position (calculated from the corners to the nearest screen edge)
                                      # Automatically fill black borders if different from the video scale. Maintains the set ratio as much as possible, even if stretched. The --autofit series and the --window-scale parameter will be invalidated with this item
#autofit=70%                          # <[W[xH]]> Initial window size and target size (in percentage or pixels) for scaling when playing files under window, default: 60%.
#autofit-larger=90%x90%               # <[W[xH]]> Maximum percentage of screen in windowed mode (e.g. initial window is too large for opening 4k video on FHD screen)
autofit-smaller=45%x30%               # <[W[xH]]> Minimum percentage of screen in windowed mode (e.g. initial window is too small for 480p video on 4k screen)
#keepaspect=no                        # default yes, by default MPV will only stretch the video to the source scale. Enabling this will always anamorphic the screen to the window size
#keepaspect-window=no                 # Default yes, by default MPV's window scale is locked to the video scale. Enable this for free window stretching behavior (black border around when keepaspect=yes)
#panscan=1.0                          # <0.0-1.0> Crop the screen to fill the window (recommended to use with keepaspect-window=no).
#pause                                # Start the player in pause state.
#loop=inf                             # <N|inf|no> Always loop the current file.
#loop-playlist=no                     # <N|inf|force|no> Loop through the playlist.
#keep-open=yes                        # <yes|default no|always> keep-open after playlist finishes, always: pause at the end of the file, don't play the next file
#keep-open-pause=no                   # <default yes|no> Pause the playlist after it finishes playing.
                                      ## --keep-open=yes, --keep-open-pause=yes Parameters are preconditions for [end] conditional configuration group to take effect.
idle=yes                              # <yes|no|default once> idle-standby (keep mpv running after playback is stopped or all files are played)
#untimed                              # Do not sleep while outputting video frames. (Can't display frame filler values correctly when turned on)
log-file=“~~/files/mpv.log”           # Record logs
ontop                                 # Put the window on top.
snap-window=yes                       # 窗口移动时捕捉窗口到屏幕边缘 (仅限 windows 系统)
#window-affinity=default              # <default default|excludefromcmcapture|monitor> Controls the window-affinity behavior of the mpv. excludefromcmcapture: MPV's window will be completely excluded from capture by an external application or screen-recorder software; monitor: black out the MPV window
hr-seek=yes # <no|absolute|default|yes|always> Select when to use exact jumps that are not limited to keyframes. This type of jumping requires decoding the video from the previous keyframe to the target position, so it may take some time depending on the decoding performance
                                      # no: disable; absolute: use precision jumps only for chapters; default: similar to absolute, but enable precision jumps in audio-only situations. May change behavior with version updates; yes: use exact skips whenever possible; always: same as yes (for compatibility)
hr-seek-framedrop=no                  # Drop frames when jumping, turn it off to help correct audio delays # Recommended setting is no for SVP framing.
save-position-on-quit=yes             # Remember playback state on exit. Cache directory is “watch_later” in settings folder by default.
                                      # You can also press the shortcut Q to save the progress when exiting.
write-filename-in-watch-later-config  # Write filename into playlist cache file
#ignore-path-in-watch-later-config    # Ignore path (i.e. only use filename) when using “watch-later” feature. (default: no)
resume-playback-check-mtime=yes       # Resume playback from the watch_later configuration subdirectory (typically ~/.config/mpv/watch_later/) if the file was modified at the same time as it was saved. This prevents misbehavior in files of the same name with different content (default: no)
watch-later-dir="~~/cache/watch_later"
                                      # 在此目录中存储 "watch_later" 文件夹，其中的文件记录 --watch-later-options 指定的选项
watch-later-options=start,vid,aid,sid
                                      # [Recommended setting is leave blank when SVP framing] Whitelist for watch later
                                      # When --save-position-on-quit=yes or using the save to watch later on exit shortcut, multiple states such as filter list, volume, rate, etc. will also be saved and restored on next startup if whitelisting is not used.
                                      # Specific parameter values: start,speed,edition,volume,mute,audio-delay,gamma,brightness,contrast,saturation,hue,deinterlace,vf,af,panscan,aid,vid,sid. sub-delay,sub-speed,sub-pos,sub-visibility,secondary-sub-visibility,sub-scale,sub-use-margins,subass-force-margins,sub-ass- vsfilter-aspect-compat,sub-ass-override,ab-loop-a,ab-loop-b,video-aspect-override
#watch-later-options-clr              # Restore playback history to disable restoration of all settings changed during playback, this fixes some known issues.
#watch-later-options-remove=contrast  # This item can be used to disable restoration of some parameters.
                                      # This item must be listed individually when specifying parameters, it is not possible to specify parameters like --watch-later-options=option1,option2,..... It is recommended to use the watch-later-options whitelist.
reset-on-next-file=vid,aid,sid,secondary-sid,vf,af,loop-file,deinterlace,contrast,brightness,gamma,saturation,hue,video-zoom,video-rotate,video-pan-x,video-pan-y,panscan,speed,audio-delay,sub-pos,sub-scale,sub-delay,sub-speed,sub-visibility,secondary-sub-visibility
                                      # Changes to be reset when playing the next file (color, picture, audio track, subtitle and filter related)
input-ipc-server=\\\. \pipe\mpvsocket # input-ipc-server=<ipc_path> enable Ipc function, Linux/MacOS: input-ipc-server=/tmp/mpvsocket
                                      # Set this to the mpvpipe value when using SVP manager.
#demuxer-max-bytes=500MiB             # Target cache size (KiB or MiB) for playing web video.
#demuxer-readahead-secs=20            # Limit the maximum cache time (in seconds) for web videos
#video-exts=3g2,3gp,asf,avi,bdmv,f4v,flv,h264,h265,iso,ifo,m2ts,m4v,mkv, #demuxer-readahead-secs=20 # Limit the maximum cache time (in seconds) for network video.mov,mp4,mp4v,mpeg,mpg,ogm,ogv,rm,rmvb,ts,vob,webm,wmv,y4m
                                      # Specify a list of extensions for the video file types to be matched in the --autocreate-playlist and --directory-filter-types options
#audio-exts=aac,ac3,aiff,ape,au,dsf,dts,flac,m4a,mid,midi,mka,mp3,mp4a,oga,ogg,opus,spx,tak,tta,wav,weba,wma,wv
                                      # Specify a list of extensions for the audio file types to be matched in options --audio-file-auto, --autocreate-playlist and --directory-filter-types
#image-exts=apng,avif,bmp,gif,j2k,jp2,jfif,jpeg,jpg,jxl,mj2,png,svg,tga,tif,tiff,webp
                                      # Specify the list of image file type extensions to match in options --cover-art-auto, --autocreate-playlist and --directory-filter-types
#directory-mode=ignore                # <default auto|recursive|lazy|ignore> Select recursive, lazy or ignore all subdirectories when opening a directory
#directory-filter-types=video         # <video,audio,image> Specify media file types to filter when opening a directory. If the list is empty, all files will be added to the playlist (default: video,audio,image)
#autocreate-playlist=same             # <default no|filter|same> Specify the behavior of automatically creating playlist when opening local files
                                      ## no: no creation, only single files are loaded; filter: creates a playlist from the parent directory containing files matching the types specified in the option --directory-filter-types; same: creates a playlist from the parent directory containing files of the same media type as the currently loaded file matching the option --*-exts files in the options --*-exts
                                      #! Note: This option is also affected by --directory-mode, if --directory-mode is not ignored a playlist with subdirectories will be created
#ordered-chapters=no                  # Ordered chapters support is enabled by default, use no-ordered-chapters to disable it.
                                      # This is support for the chapter-links feature of mkv. If disabled, mpv will not load or search for video clips from other files, and will ignore any chapter ordering specified in the master file.
metadata-codepage=auto                # Specify the coding of various input metadata (default: auto). This affects how file labels, chapter titles, etc. are interpreted. For example, it can be set to auto to enable auto-detection of encodings (non-UTF-8 encodings are an obscure and marginal use case)
msg-level=all=info                    # <module1=level1,module2=level2,... > Controls the level of detail each module outputs to the console log, which also affects the log file. available level: <no|fatal|error|warn|info|status default|verbose|debug|trace
                                      # The example shows only info and above. For more information and use cases, please refer to the official manual: https://mpv.io/manual/master/#options-msg-level

#######
# OSD #
#######

##⇘⇘OSD is On-Screen-Display, which is usually the information that pops up on the screen. osc is on-screen-controller, which in MPV refers to an easy-to-use interface.
## More customized queries https://mpv.io/manual/master/#osd

no-osd-bar
osd-on-seek=msg-bar                   # <no,bar,msg,msg-bar> Type of message to display when jumping timelines
osd-bar-w=100
osd-bar-h=2
osd-bar-align-y=-1
# osd-fonts-dir=~~/fonts              # Specify the directory where OSD fonts should be found, the example is the default value.
osd-font=“Noto Sans Mono CJK SC”      # Specify the OSD font.
osd-font-size=24                      # change OSD font size (global, affects text displayed by multiple functions) (default: 55)
osd-color=“#FFFFFFFF”                 # OSD text main color
osd-border-size=1.0                   # OSD text border size
osd-border-color=“#FF000000”          # OSD text border color
osd-shadow-offset=0.5                 # OSD text subtitle shadow size
osd-shadow-color=“#FF000000”          # OSD text subtitle shadow/background color
# osd-border-style=outline-and-shadow # <default outline-and-shadow|opaque-box|background-box> text caption border style
                                      # outline-and-shadow: draw outline and shadow; opaque-box: draw outline and shadow; draw outline and shadow as an opaque box that wraps tightly around each line of text; background-box: draw a background box that frames all lines of text
#osd-spacing=1                        # OSD text caption word spacing
#osd-blur=0.5                         # Edge blur of OSD text caption <0..20.0 
#osd-bar-border-size=1.2              # OSD bar border size
#osd-playlist-entry=filename          # <default title|filename|both> Specify playlist to show media title or filename or both.
#osd-playing-msg="${?demuxer-via-network==yes:${media-title}}${?demuxer-via-network==no:${filename}} ${!playlist-count==1:list:${ playlist-pos-1}/${playlist-count}}”
                                      # Information to be displayed briefly when each file starts playing. Preset to show filename
osd-status-msg=${playback-time/full} / ${duration/full} (${percent-pos}%)\nframe: ${estimated-frame-number} / ${estimated-frame-count}
                                      # Information displayed by the OSD when jumping
osd-fractions=yes                     # Display OSD time in seconds (milliseconds precision), useful to see the exact timestamp of a video frame
osd-duration=2000                     # Set (globally) the duration (in milliseconds) of the OSD text message (default: 1000)
#osd-playing-msg-duration=2000        # Duration of the --osd-playing-msg text, if this is not set then the global duration is used

########
# Video #
########

### Video-related settings are set using a configuration group, the following is only an example of parameter descriptions, the specific settings are described in the [General Parameter Supplement] and [Configuration Group] of the relevant configuration group parameters below.


#icc-profile="~~/icc/ITU-RBT709ReferenceDisplay.icc” 
                                      # Load the ICC profile and use it to convert video RGB to on-screen output, self-select the icc profile for the corresponding color gamut of your monitor. This option overrides the --target-prim, --target-trc, and --icc-profile-auto options.
                                      ## Since the mpv color target is bt.1886 (close to gamma=2.4), when the --icc-profile-auto parameter is enabled to use the system-specified (srgb) calibration file, the (srgb) calibration file gamma curve is usually gamma2.2, which will result in the mpv screen displaying abnormally.
                                      ## To solve the above problem, it is better to manually specify the corresponding monitor gamma file whose gamma curve is gamma2.4, the sample parameters can be used for reference.
#icc-profile-auto # Enable if you have done professional color correction (corresponding icm color correction file exists in system directory). Wide gamut screens without calibration should be specified manually target-prim=<value
                                      # Enable color management to allow video in standard color space to display relatively correct colors on wide gamut monitors, disable to restore oversaturation.
                                      ## Currently mpv is using icc color management because the calibration curve target is bt.1886, which will cause most of the displays to have problems such as off-color and dark picture https://github.com/mpv-player/mpv/issues/8009
                                      ## Enable the parameter --icc-force-contrast=1000, it can solve the problem that some color correction files have very high contrast, resulting in a dark screen (for oled screen, test the value by yourself). #The --icc-force-contrast=1000 can solve the problem of high contrast in some color correction files, resulting in a darker image. Note that this still doesn't solve the problem of darker hdr mapping.
                                      # Or use the --target-prim=<value> parameter to manage the color space.
#icc-intent=0 # Color conversion mapping intent for ICC, 0: perceptual, 1: relative chroma (default), 2: saturation, 3: absolute chroma. Recommended perceptual mapping https://github.com/mpv-player/mpv/issues/8009#issuecomment-923607273
#icc-force-contrast=1000 # <no|0-1000000|inf> Color management. Override the static contrast data in ICC file, default no is no override, oled can be set to inf which means infinite (if this data is lost in ICC, it will fall back to 1000 automatically), this item only affects the content display of bt.1886.
                                      ## Normal LCD generally use 1000 (based on panel's native data); users with OLED display devices try to use 1000000 or special value inf. LCD set to 1000 can solve the problem that some color correction files have very high contrast, resulting in the black part being too dark.
#icc-3dlut-size=64x64x64 # <default auto|RxGxB> The size of the 3D LUTs generated from the ICC profiles in each dimension. Range <2--512>
#icc-cache=no # Whether to store 3dlut cache locally for ICC profiles, enabled by default
#icc-cache-dir=“~~/cache/icc_cache” # Specify a directory to store and load 3D LUTs created from ICC profiles. this can be used to speed up loading. Note that these files contain uncompressed LUTs. their size is dependent on --icc-3dlut-size and can be very large. Note: This is not automatically cleared, so old, unused cached files may stay indefinitely!
                                      ## Cache-generated 3dlut is supposed to be accelerated, but lack of hard disk IO performance can be counterproductive, so disable it or not depending on the situation.
#use-embedded-icc-profile=no # Load embedded ICC profiles contained in media files (e.g. PNG images). (default: yes). Note that this option is only valid under --vo=gpu if you also use a display ICC profile (--icc-profile or --icc-profile-auto)


#target-prim=auto # Wide gamut screens should set this parameter to a specific value in order to solve the problem of color oversaturation, when not using ICC color management, the video color will adapt to this color space. srgb screens don't need to change this default value (bt.709), other examples: 99% of argb screens write adobe, 90%+ of p3 gamut screens write dci- p3
                                      # Using the --target-prim=<value> parameter to manage the color space can lead to off-color problems under hdr. Enabling the --gamut-mapping-mode=clip parameter for hdr mapping can solve this problem https://github.com/mpv-player/mpv/issues/ 9071
#target-gamut=dci-p3 # Explicitly limit the color gamut of the monitor. You can use this option to limit the output gamut, e.g. DCIP3-in-BT.2020; set `-target-prim` to the color space specification and `-target-gamut` to the output gamut to limit. Depends on `--target-prim`. (applies only to `--vo=gpu-next`)
#target-trc=gamma2.2 # Specify the transmission characteristics (gamma) of the display, effective when icc color management is not used. Default auto: use gamma2.2 under --vo=gpu for hdr, leave it alone for sdr; always use bt.1886 curve under --vo=gpu-next (assuming infinite contrast, i.e., oled)
                                      # recommend to always use gamma2.2 curve for non oled and hdr monitors to avoid dark mpv. hdr passthrough should use auto or pq value for this item
# target-contrast=auto # <auto|10-1000000|inf> Specify the measured contrast of the output display. This is used in conjunction with --target-peak to calculate the display black point for black point compensation during HDR tone mapping.
                                      #target-peak=“auto” Defaults to auto, which assumes a 1000:1 contrast ratio (considered typical for SDR displays) and will have infinite contrast when HDR is passed through. inf Contrast specifies the ability to display with a perfect black level, i.e., OLED display
#target-peak=auto # <auto|nits> Specifies the measured peak brightness of the output display. Affected by --target-trc parameter, default value auto: use appropriate value according to trc parameter, sdr defaults to 203, under hdr it is recommended to specify specific nits value according to actual display effect
                                      # When using icc color management, if nits is set higher than the default (203), the monitor will be treated as an hdr screen by the mpv.
                                      # --vo=gpu-next Do not change this parameter if possible to avoid anomalous performance.
##⇘⇘⇘ The following are 3dlut related parameters
#lut=<file> # [Effective when --vo=gpu-next] to specify a custom LUT (in Adobe .cube format) to apply to colors as part of the color conversion. The exact way depends on --lut-type
#lut-type=conversion # [Effective when --lut=<file> is enabled], <default auto|native|normalized|conversion>, controls the way color values are passed in and out of the LUT specified as --lut
                                      # auto: automatically select the LUT from the tag's metadata, otherwise fallback to the system (default); native: apply to the original image content in its native RGB color space before converting to the output color space (non-linear light)
                                      # normalized: normalized RGB image content applied in linear light before conversion to the output color space
                                      # conversion: full-replacement conversion from image color space to output color space. If such a LUT exists, it has the highest priority and overrides any ICC profile and options related to tone mapping and output chroma (--target-prim, --target-trc, etc.)
#target-lut=<file> # [Effective when --vo=gpu-next], specifies a custom LUT file (in Adobe .cube format) to be applied to the color before it is displayed on the screen
                                      # After encoding into the target color space, this LUT provides values in normalized RGB form, so it will be processed after the --target-trc parameter
#image-lut=<file> # [Effective when --vo=gpu-next], specify a custom LUT file (Adobe .cube format) to apply color to during image decoding. the exact way the LUT is applied depends on --image-lut-type
#image-lut-type=conversion # [Effective when --image-lut=<file> is enabled] , <default auto|native|normalized|conversion> , controls the way color values are passed in and out of the LUT specified as --image-lut
                                      # auto: automatically select the LUT from the tagged metadata, otherwise fallback to the system (default); native: apply to the original image content in the original color space before decoding to RGB. For example, for HDR10 images, this would enter PQ-encoded YCbCr values in the range 0.0-1.0
                                      # normalized: applied to normalized RGB image content after decoding from its native color encoding, but before linearization
                                      # conversion: Completely replaces color decoding. This type of LUT should ingest the native color space of the image and output normalized non-linear RGB
##⇘⇘⇘ The following are hdr related parameters
#inverse-tone-mapping=yes # [Effective when --vo=gpu-next], if set, allows inverse tone mapping (extending SDR to HDR), valid only on HDR devices. The use of partial tone mapping curves is not supported, use with caution!
#target-colorspace-hint=yes # [Effective when --vo=gpu-next], if possible, automatically configures the monitor's output colorspace to pass through the input values of the media stream (e.g. HDR pass-through). Requires driver support
                                      # --vo=gpu-next --gpu-api=vulkan available in fullscreen (Windows limitation), --gpu-api=d3d11 requires manual switching of monitor HDR mode (Windows limitation).
#tone-mapping=hable # tone-mapping algorithm, other recommended ones are clip|mobius|reinhard|hable|bt.2390|gamma|spline|bt.2446a|st2094-10|st2094-40, auto by default, choose based on internal heuristics Optimal curve (actually spline under --vo=gpu-next)
                                      ## Algorithms spline, bt.2446a, st2094-10 and st2094-40 are only available with --vo=gpu-next
                                      ## hable or bt.2390 is recommended for testing with vo=gpu. hable retains more detail, bt.2390 has better contrast.
                                      ## For vo=gpu-next it is currently recommended to test spline, hable and bt.2446a. spline mapping algorithm with --hdr-compute-peak has the best tone-mapping performance inside mpv so far.
#tone-mapping-param=0.3 # Small parameter for tone mapping algorithm. hable and bt.2446a have no parameter; bt.2390 has no parameter with --vo=gpu and defaults to 1.0 with -vo=gpu-next; st2094-10 defaults to 0.7/mobius and spline defaults to 0.5/st2094-10; reinhard defaults to 0.5/hdr-compute-peak. reinhard default 0.5/gamma default 1.8 (-vo=gpu-next default 0.3)
                                      # -vo=gpu-next Recommended value when using bt.2390 under -vo=gpu-next: 2.0 or 0.5 in ITU-R specification.
#tone-mapping-max-boost=1.0 # <1.0-10.0> Maximum exposure for average brightness, default: 1.0. Increasing this value will show hidden details in dark areas, too high will make dark areas look unnaturally bright. Try 1.0,1.33, available only with --vo=gpu.
#gamut-mapping-mode=clip # <default auto|clip|perceptual|relative|saturation|absolute|linear|warn|desaturate|darken> Specify the desired target display gamut after any tone mapping is done. processing, default auto: automatically selects the best mode (perceptual for practical applications)
                                      # clip: RGB per-channel hard clipping to the target gamut, recommended for priority use; perceptual: Performs perceptually balanced gamut mapping using a soft knee with a roll-off clipping region function and a hue shift function to maintain saturation
                                      # relative: performs relative colorimetric clipping while maintaining the exponential relationship between luminance and chroma; saturation: performs simple RGB->RGB saturation mapping. Never clips, but will distort all hues and/or cause a faded look
                                      # absolute: performs absolute colorimetric clipping. Like 'relative' but really doesn't fit the white point; linear: reduces image saturation linearly/uniformly to bring the entire image into the target color gamut
                                      # warn: just highlight out-of-gamut pixels; desaturate: performs constant brightness colorimetric clipping, reducing color saturation towards whites until they're in range; darken: uniformly darkens the input slightly to prevent clipping and highlighting when blown out, then colorimetrically clamps to the input gamut boundaries, with a slight bias to keep the luminance in chroma
#hdr-contrast-recovery=0.30 # <0.0-2.0> Enables the HDR contrast recovery algorithm, which is designed to enhance the contrast of HDR video after tone mapping. Applies only to --vo=gpu-next
                                      # Intensity (default: 0.0) Indicates the degree of contrast restoration, 0.0 means completely disabled, 1.0 means 100% intensity. Values greater than 1.0 are allowed, but may result in oversharpening!
                                      ## mpv Default value for this parameter is 0.30 in built-in configuration group gpu-hq.
#hdr-contrast-smoothness=3.5 # <1.0-100.0> Enable the HDR contrast smoothing algorithm, which is designed to enhance the smoothness of HDR video after tone mapping. Applies only to --vo=gpu-next
                                      # Smoothness (default: 3.5) indicates how much the HDR source is underpassed for contrast information, a value of 2.0 corresponds to a 2x reduction. Users with low DPI monitors (<= 100) may want to decrease this value, while users with very high DPI monitors (“retina”) may want to increase it!
#hdr-compute-peak=yes # <default auto|yes|no> This option basically provides dynamic scene-based tone mapping (older cards don't support this parameter, and the image will be white), which may cause the brightness to flicker as the scene changes
                                      # Currently with --hdr-compute-peak enabled some tone mapping curves (bt.2390, reinhard) are prone to loss of highlight detail or overexposure; some tone mapping curves show significant improvement (spline, bt.2446a, humble).
#hdr-peak-decay-rate=20 # [Effective when --hdr-compute-peak=yes] for the decay rate of the HDR peak detection algorithm (<0.0-1000.0>default: 20.0)
#hdr-scene-threshold-low=1.0 # [Effective when --hdr-compute-peak=yes] , the lower threshold (dB) for luminance difference to be considered as a scene change (<0.0-100.0>default: 1.0)
#hdr-scene-threshold-high=3.0 # [Effective when --hdr-compute-peak=yes] , the upper threshold (dB) of the luminance difference will be considered as a scene change (<0.0-100.0> default: 3.0)
#hdr-peak-percentile=100 # Effective when [--hdr-compute-peak=yes] and [-vo=gpu-next]. (<0.0-100.0> Default: 100.0) Specifies which percentile of the input image luminance histogram is to be considered as the true peak
                                      # If set to 100 (default), the brightest pixels are measured. Otherwise, the top of the frequency distribution is gradually cut off. Setting it too low results in clipping of very bright details, but can improve the dynamic brightness of a range of scenes with very bright isolated highlights. values other than 100 come with a small performance loss
                                      ## If you feel that the brightness of the scene is always jumping around you can ignore some of the spikes by lowering this parameter, values below 99.7 are not recommended.
                                      ## The default value for this parameter in the mpv built-in configuration group gpu-hq is 99.995.
## allow-delayed-peak-detect=no ## Effective when [--hdr-compute-peak=yes] and [-vo=gpu-next], defaults to no. With --hdr-compute-peak, it is permissible to delay detected peaks by one frame if this is in the interest of performance.
                                      # Especially when advanced rendering is not required, this can avoid unnecessary FBO processing. Note that -vo=gpu delays peak processing by default


# video-sync=display-resample # Class “ReClock” role, change to synchronize the video frame with the monitor refresh rate, automatically adjust the audio speed to compensate for the offset. # Recommended to disable for SVP framing.
                                      # Default value audio (synchronized with audio/system clock) is usually the most compatible but has occasional frame loss and repetition.
#video-sync-max-video-change=5 # [only works if --video-sync=display-xxxx] Default: 1
#interpolation # [Effective only if --video-sync=display-xxxx] Reduces lag (also known as frame jitter) caused by mismatch between video fps and display refresh rate. # Recommended to turn off SVP framing.
                                      # interpolation. See https://mpv.io/manual/master/#options-tscale for details.
                                      # See https://github.com/mpv-player/mpv/issues/2685 for some discussion
#interpolation-preserve=no            # [当 --vo=gpu-next 时生效] ，默认 yes。即使更改渲染器参数，也保留前一帧的插值结果。裁剪和视频放置相关的选项除外，它们总是使缓存无效
                                      # Enabling this option will make dynamic updates set by the renderer smoother, but at the cost of slightly higher latency when responding to such changes.
# tscale=oversample  # [Effective only when --interpolation is enabled] Selects the time-domain interpolation algorithm (not MEMC motion compensation). # It is recommended to disable it when using SVP frame interpolation.
                                      # The default interpolation algorithm is oversample. In general, mitchell is smoother (though not comparable to SVP frame interpolation), but it will be blurry. Oversample has no blur but is not smooth (it basically restores the look of 24 frames, similar to MADVR's smoothmotion), and triangle is in between. Catmull_rom is relatively sharp.
                                      # Please choose according to your own needs.

#tscale-radius=1.0                    # Sets the radius of the adjustable filter, must be a floating point number between 0.5 and 16.0. If not specified, it defaults to the filter's preferred radius. Not applicable for every scaler and VO combination.
#tscale-clamp=0.0                     # <0.0-1.0> Specifies the bias to be multiplied by negative coefficients. Specifying --scale-clamp=1 effectively removes all negative weights, effectively limiting the value range to [0-1]. Values between 0.0 and 1.0 can be specified to only moderately reduce negative weights.
                                      # The default value for tscale is 1.0, which effectively reduces the ringing artifacts produced by tscale. Other scale algorithms have a default value of 0.0.
#tscale-antiring=1.0                  # Sets the anti-ringing strength. This attempts to eliminate ringing but introduces other artifacts in the process. Must be a floating-point number between 0.0 and 1.0. The default value of 0.0 completely disables anti-ringing.
                                      # Note that this does not affect special filters like bilinear and bicubic_fast.
#dither-depth=auto                    # <N|no|default auto> Enables dithering to compensate for color conversion loss. Use 8 for 8-bit displays, 10 for 10-bit displays. The value of auto depends on --gpu-api and may not always be correct; for Vulkan, you must manually specify 10bit. See issue status: https://github.com/mpv-player/mpv/issues/8554.
                                      # If using a 10-bit display with 8-bit dithering causes output issues, change the value back to 8.
#dither=fruit                         # <default fruit|ordered|error-diffusion|no> [Effective when --dither-depth is enabled], selects the dithering algorithm.
                                      # The error-diffusion algorithm is the best but also the most power-consuming. If performance is insufficient, it will automatically fall back to the fruit algorithm.
#dither-size-fruit=6                  # [Effective when --dither=fruit is enabled], <2-8> Sets the size of the dithering matrix (default value: 6). The actual matrix size is (2^N)x(2^N), so a value of 6 corresponds to a 64x64 matrix.
#error-diffusion=sierra-lite          # <simple|default sierra-lite|floyd-steinberg|atkinson> [Effective when --dither=error-diffusion is enabled], sets the kernel used by the error-diffusion algorithm.
#temporal-dither                      # [Effective when --dither-depth is enabled], Enables temporal dithering (inter-frame dithering).
                                      # This varies the dithering pattern across 8 different modes between frames, potentially causing flicker on LCD screens.
#temporal-dither-period=1             # <1-128> [Effective when --temporal-dither is enabled], Determines the frequency at which the dithering pattern updates when in use. A value of 1 (default) will update on every video frame.

#scale=ewa_hanning                    # Upscaling algorithm, the default algorithm is lanczos. Recommended algorithms are --ewa_lanczos, --ewa_lanczossharp, and --ewa_hanning, which are variations of the jinc algorithm.
                                      # Compared to other jinc algorithms, ewa_hanning reduces ringing artifacts.
                                      # Among the built-in scale algorithms, the spline36 algorithm is better suited for live-action images, while the ewa algorithms are more suitable for anime images.
#scale-radius=3.2383154841662362      # Sets the radius of the adjustable filter, must be a floating-point number between 0.5 and 16.0. If not specified, it defaults to the filter's preferred radius. Not applicable for every scaler and VO combination.
#scale-antiring=1.0                   # Sets the anti-ringing strength. This attempts to eliminate ringing but introduces other artifacts in the process. Must be a floating-point number between 0.0 and 1.0. The default value of 0.0 completely disables anti-ringing.
                                      # Note that this does not affect special filters like bilinear and bicubic_fast.
                                      ## There is generally no need to enable this option when using external upscale shaders.
#cscale=bilinear                      # Chroma scaling algorithm; if not specified, it defaults to --scale (not recommended). It is recommended to use the bilinear algorithm. The difference in perception of different chroma upscaling algorithms is usually insignificant, so it is generally unnecessary to invest too much performance.
                                      # In actual testing, it was found that the bilinear algorithm is more suitable for poorer sources, while the catmull_rom algorithm is better for higher-quality sources.
                                      # Although "KrigBilateral.glsl" has the best effect, it may cause excessive power consumption with certain sources (VFR) and shader combinations.
#cscale-radius=3                      # Sets the radius of the adjustable filter, must be a floating-point number between 0.5 and 16.0. If not specified, it defaults to the filter's preferred radius. Not applicable for every scaler and VO combination.
#cscale-antiring=1.0                  # Sets the anti-ringing strength. This attempts to eliminate ringing but introduces other artifacts in the process. Must be a floating-point number between 0.0 and 1.0. The default value of 0.0 completely disables anti-ringing.
                                      # Note that this does not affect special filters like bilinear and bicubic_fast.
                                      ## There is generally no need to enable this option when using external cscale shaders.
#dscale=catmull_rom                   # Downscaling algorithm. The default algorithm is hermite.
                                      # When only using the built-in dscale algorithm, it is recommended to use bicubic (with the catmull_rom variant) or ewa_hanning. You can also test the effects of robidouxsharp, ewa_robidouxsharp, and other algorithms.
#dscale-radius=1                      # Sets the radius of the adjustable filter, must be a floating-point number between 0.5 and 16.0. If not specified, it defaults to the filter's preferred radius. Not applicable for every scaler and VO combination.
#dcale-antiring=1.0                   # Sets the anti-ringing strength. This attempts to eliminate ringing but introduces other artifacts in the process. Must be a floating-point number between 0.0 and 1.0. The default value of 0.0 completely disables anti-ringing.
                                      # Note that this does not affect special filters like bilinear and bicubic_fast.
                                      ## There is generally no need to enable this option when using external dscale shaders.
#linear-upscaling=no                  # [Requires --fbo-format to specify 16-bit or higher precision; not compatible with --sigmoid-upscaling] (No effect on HDR content).
#sigmoid-upscaling=no                 # [Not compatible with --linear-upscaling] Non-linear color conversion during upscaling, helps to avoid strong ringing artifacts. Enabled by default.
                                      ## Developers recommend prioritizing --sigmoid-upscaling or not using either of the above options (i.e., scaling in gamma light).
                                      ## There is generally no need to enable this option when using external upscale shaders.
#correct-downscaling=yes              # [Ineffective when --dscale=bilinear] Enhances the quality of downscaling (anti-aliasing), may cause slight distortion in non-standard aspect ratio videos. Enabled by default.
                                      ## Can be replaced by the better "SSimDownscaler.glsl" shader solution.
#linear-downscaling=no                # [Disable when using "SSimDownscaler.glsl"; requires --fbo-format to specify 16-bit or higher precision] (No effect on HDR content). Enabled by default.
                                      ## The two --linear- parameters correspond to "scale in linear light" in MADVR, and linear processing during downscaling can improve color contrast accuracy and suppress ringing artifacts generated by some algorithms.
#scaler-resizes-only=no               # Default yes, uses the bilinear algorithm to perfectly restore when no scaling is performed. When disabled, it processes with the specified scale value even if the size is unchanged, which can correct the half-pixel offset introduced by nnedi3 and ravu (but it's unnecessary).
#deband=no                            # Removes color banding. Does not need to be always enabled, can be manually enabled depending on the source.
#deband-iterations=1                  # <0-16> Number of debanding iterations, higher values increase intensity but also require more processing power. Values >4 are ineffective (default 1), 3-4 are equivalent to MADVR's deband set to high.
#deband-threshold=48                  # <0-4096> Cutoff threshold for the filter, higher numbers increase intensity but gradually reduce image detail (default 48).
#deband-range=16                      # <1-64> Initial radius, higher values increase debanding strength and performance usage; smaller values result in smoother output (default value 16). If increasing --deband-iterations, reduce this value to compensate.
#deband-grain=32                      # <0-4096> Adds extra dynamic noise to help conceal remaining quantization artifacts (default 32).

########
# Audio #
########

audio-device=auto                     # This option is used to specify the audio output device when starting.
                                      # Example of a specific value --audio-device="wasapi/{xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx}", device name can be found in the official manual.
audio-channels=7.1,5.1,stereo         # <Default value auto-safe|auto|layouts|stereo> If some channels do not produce sound when playing multi-channel movies on a stereo system, try forcing it to stereo.
                                      # Note: This option must be set to the actual channel value or priority order of the audio output device. The default auto-safe and auto options in mpv can result in missing channels when downmixing multi-channel audio, so they are not recommended.
                                      # Example parameter 7.1,5.1,stereo represents the common priority order of audio output devices and will automatically fall back to the output value that matches the actual audio output device (most commonly stereo dual-channel devices).
                                      # If the background sound is too loud and the dialogue is too quiet when downmixing multi-channel audio to stereo, try this: https://github.com/mpv-player/mpv/issues/6563 
ao=wasapi                             # --ao=<driver1,driver2,...[,]> Specifies the priority list of audio output drivers to be used. If the list has a trailing ',', mpv will fall back to drivers not included in the list.
                                      # Recommended for Windows: wasapi; for Linux: alsa (use with the --audio-channels=auto parameter); for macOS: coreaudio or coreaudio_exclusive.
                                      # In tests, if another software exclusively occupies the audio channel, opening MPV later can result in extreme stuttering of the video.
#audio-exclusive=yes                  # Exclusive audio channel. If there is an audio stuttering problem, you can disable this option or the above option.
#volume=100                           # Player startup volume. 0 is mute, the default is 100.
#volume-max=130                       # <100.0-1000.0> Maximum volume. The default is 130 (130 volume is about twice as loud as 100, 1.3^3≈2).
#audio-pitch-correction=no            # [A prerequisite for --af=scaletempo/scaletempo2/rubberband] Pitch correction during speed changes, default is yes. Automatically inserts one of the first three audio filters (more information can be found in the filters section).
#audio-delay=0.04                     # Set audio delay if necessary.
#audio-samplerate=96000               # Force resample to 96kHz, 24bit (requires hardware support from the audio device). Note! Not necessary.
#audio-format=s32
#audio-normalize-downmix=yes          # Enable/disable normalization when downmixing surround audio to stereo (default: no). If this feature is disabled, downmixing may result in clipping. If this feature is enabled, the output may be too quiet. It depends on the source audio.
#replaygain=track                     # <Default no|track|album> Adjusts the volume gain based on the replay gain values stored in the file metadata.
                                      # no: No adjustment; track: Apply track gain; album: Apply album gain if available, otherwise fallback to track gain.
#ad-lavc-downmix=yes                  # <yes|default no> Whether to request audio channel downmixing from the decoder. Some decoders (e.g., AC-3, AAC, and DTS) can remix the audio during decoding.
gapless-audio=no                      # <no|yes|weak default> Attempt to play continuous audio files without muting or interruption when changing files. The default value weak initializes the audio output when the audio format changes.
                                      # Setting to yes can cause files to save with the same sample rate, which may degrade audio quality.
#audio-file-auto-exts=aac,ac3,dts,eac3,flac,m4a,mka,mp3,ogg,opus,thd,wav,wv
                                      # Specifies the file extensions to try matching when using the "audio-file-auto" option. The example is the default value.
audio-file-auto=fuzzy                 # <Default no|exact|fuzzy|all> Specifies how to automatically load external audio tracks.
                                      # no: Disable; exact: Automatically load external audio tracks with extensions that match the BCP 47 language tag syntax; fuzzy: Automatically load external audio tracks containing the video filename; all: Automatically load all detected audio tracks.
audio-file-paths=audio;audios;**      # <path1:path2:...> Search for matching audio tracks in specified additional directories, supports relative and absolute paths.
                                      # ":" should be replaced with ";" on Windows.
                                      # You can use fuzzydir.lua to enhance the path configuration, adding "**" allows automatic loading of all matching subdirectory audio tracks in the same directory as the video.
alang=japanese,jpn,jap,ja,jp,english,eng,en
                                      # Sets the preferred audio track language, but MPV prefers to load external tracks.
#aid=1                                # Currently unable to select tracks based on other metadata. MPV has a preference for external tracks, often selecting unwanted audio tracks.
                                      # It is recommended to use the trackselect.lua script to automatically select specified audio tracks based on metadata: https://github.com/po5/trackselect


########
# Subtitle #
########

#sub-font-provider=fontconfig                 # <Default auto|none|fontconfig> Font rendering API option, recommended to use auto on Windows, as DirectWrite offers the best performance.
                                              # auto: Automatically selects the system font rendering interface: DirectWrite for Windows, fontconfig for Linux, CoreText for macOS.
                                              # none: Ignores the font specified in the subtitle and uses the sub-font parameter without changing the subtitle style (ineffective for internal subtitles).
                                              # fontconfig: Uses the fonts.conf file in the mpv.conf directory to load fonts. Custom font paths can be specified with <dir>CUSTOMFONTDIR</dir>.
                                              # On Windows, using fontconfig will load all fonts in the directory into memory initially, and then load on demand, which is extremely slow and not recommended.
#sub-ass-override=no                          # <yes|no|Default scale|force|strip> Sets subtitle rendering mode and controls whether to apply style overrides.
                                              ## no: Disable; yes: Apply all --sub-ass-* style override options; force: Force apply all --sub-* options; scale: Similar to yes but also applies --sub-scale; strip: Removes all ASS tags and styles, affecting PGS and SRT subtitles as well.
#sub-scale-by-window=no                       # Whether to scale subtitles according to the window size (Default: yes). Affects plain text and ASS format subtitles.
#sub-scale-with-window=no                     # Makes the subtitle font size relative to the window, not the video (Default: yes). Only affects plain text subtitles.
#sub-ass-scale-with-window=yes                # Similar to --sub-scale-with-window but only affects ASS format subtitles (Default: no). In some cases, this may cause ASS styles to be misaligned.
#embeddedfonts=no                             # Use fonts embedded in Matroska container files and ASS scripts (Default: yes).
sub-codepage=gb18030                          # Specifies the code page to use when decoding text subtitles. The default is auto: Automatically detect subtitle encoding, trying UTF-8 first, then uchardet if UTF-8 fails (mpv must be built with uchardet support).
                                              ## Example: Try GB18030 encoding first if subtitle encoding detection fails for non-UTF-8, which is suitable for Chinese subtitles.
                                              ## Note: This only applies to text subtitle files. Other types of subtitles (especially subtitles in mkv files) are always assumed to be UTF-8.
#sub-auto-exts=ass,idx,lrc,mks,pgs,rt,sbv,scc,smi,srt,ssa,sub,sup,utf,utf-8,utf8,vtt
                                              # Specifies the subtitle file extensions to try matching when using the "sub-auto" option. The example is the default value.
sub-auto=fuzzy                                # <no|Default exact|fuzzy|all> Specifies how to automatically load external subtitles.
                                              ## no: Disable; exact: Automatically load external subtitles with extensions that match the BCP 47 language tag syntax; fuzzy: Automatically load external subtitles containing the video filename; all: Automatically load all detected subtitles.
sub-file-paths=sub;subs;subtitles;字幕;**      # <path1:path2:...> Search for matching subtitles in specified additional directories, supports relative and absolute paths.
                                              ## ":" should be replaced with ";" on Windows.
                                              ## You can use fuzzydir.lua to enhance the path configuration, adding "**" allows automatic loading of all matching subdirectory subtitles in the same directory as the video.
slang=chs,sc,zh-CN,zh-Hans,cht,tc,zh-Hant,zh-HK,zh-TW,chi,zho,zh
                                              ## Sets the preferred subtitle language, but MPV prefers to load external tracks.
#sid=1                                        ## Currently unable to select tracks based on other metadata. MPV defaults to preferring external tracks, often selecting unwanted subtitles.
                                              ## It is recommended to use the sub-select.lua script to automatically select specified subtitles based on metadata: https://github.com/CogentRedTester/mpv-sub-select
#subs-fallback=default                        # <yes|Default default|no> If no existing subtitle tracks match the --slang criteria, fallback to other subtitles. The default value means only tracks marked as "default" are selected.
#subs-fallback-forced=no                      # <Default yes|always|no> When automatically selecting subtitle tracks, choose a forced track that matches the selected audio track's language if no track matches your preferred language.
#subs-with-matching-audio=yes                 # <Default yes|forced|no> When automatically selecting subtitle tracks, even if the selected audio track matches your preferred subtitle language, select a full/non-forced subtitle track.
#subs-match-os-language=yes                   # <Default yes|no> When automatically selecting subtitle tracks, choose a track that matches the operating system language if the audio stream is in another language. Note that this will be completely ignored if "--slang" is set.

#blend-subtitles=yes                          # <yes|video|Default no> Blends subtitles onto the video frame before interpolation and color management. The video value is similar to yes but draws subtitles at the video's original resolution and scales them with the video.
                                              # The --blend-subtitles=video parameter is deprecated under --vo=gpu-next.
                                              # Enabling this option limits subtitles to the visible portion of the video (they cannot appear in the black area below the video).
                                              # This also makes subtitles affected by --icc-profile, --target-prim, --target-trc, --interpolation, --gamma-factor, and --glsl-shaders.
                                              # When using --interpolation, combining it with an appropriate tscale algorithm can improve subtitle performance.
                                              ## Issue status under HDR: https://github.com/mpv-player/mpv/issues/6368 (static mapping is unaffected).
#sub-ass-force-margins=yes                    # [Ineffective when --blend-subtitles=yes/video] The default value is no. When set to yes, ASS subtitles will output on the black border as much as possible. Not recommended to be always on, as it can break some ASS pos tag effects. It's better to toggle this option with a hotkey.
#stretch-image-subs-to-screen=yes             # Forces stretching graphic subtitles to the scaled resolution instead of the reference video resolution, allowing PGS to output in the black border. Note: This may degrade the display quality.
                                              # Note: If the video aspect ratio and PGS subtitle aspect ratio are inconsistent, enabling this option is the only way to get correct subtitle display (most PGS subtitles embedded in domestic movies have this problem).
##⇘⇘ Subtitles not displaying correctly and unlike xy-subfilter? Try enabling the following settings.
sub-ass-vsfilter-aspect-compat=no             # <Default yes|no> Stretches ASS subtitles when playing videos with unconventional aspect ratios to mimic traditional vsfilter behavior.
#sub-ass-vsfilter-color-compat=full           # <Default basic|full|force-601|no> Mimics (xy-)vsfilter behavior by converting subtitle color space.
#sub-ass-vsfilter-blur-compat=no              # <Default yes|no> Scales the "\blur" tag based on video resolution instead of the .ass specified resolution. Improves compatibility with vsfilter rendering behavior, affecting the scaling behavior of "\blur," "\border," "\shadow," and other ASS tags.
                                              ## Known ASS issues: https://github.com/mpv-player/mpv/issues/9858, "\fry" tag scaling error, can be fixed by toggling the state with a hotkey.
                                              ## This option may cause scaling errors in the "\blur," "\border," "\shadow," and other ASS tags when the video resolution exceeds 1080p. The same solution applies as above.
#sub-vsfilter-bidi-compat=yes                 # <yes|Default no> When enabled, sets implicit bidirectional detection to "ltr" instead of "auto" to match ASS's default behavior. Should only be enabled when there are errors in subtitle bidirectional detection.
                                              ## This only affects plain text (non-ASS) subtitles.
#sub-ass-style-overrides-append=Encoding=-1   # Force style: Enable auto-detection. Can be used to fix character encoding and text direction recognition errors, and improve rendering of some ASS tags.
#sub-ass-style-overrides-append=ScaledBorderAndShadow=no
                                              # Force style: Do not scale borders and shadows. Prevents so-called "heavy ink" style https://github.com/libass/libass/issues/287.
                                              # Note: This issue has been fixed in git versions of libass after 0.15.2.
#sub-gauss=1.5                                # <0.0-3.0> Applies Gaussian blur to image subtitles (Default value: 0). This helps make pixelated DVD/Vobsub subtitles look better.
#sub-gray=yes                                 # Converts image subtitles to grayscale. This can help make yellow DVD/Vobsubs look better.
#sub-fix-timingyes                            # Adjusts subtitle timing to remove small gaps or overlaps between subtitles (if the difference is less than 210 ms, the gap or overlap is removed). In some cases, this may make things worse, so you can try disabling this option. Default is disabled.
#sub-stretch-durations=yes                    # Extends the duration of subtitles so that they end when the next subtitle starts. This may help with subtitles that erroneously have zero duration, but it can also break normal subtitles. Default is disabled.
#sub-fps                                      # Specifies the frame rate of the subtitle file (Default: video fps). Only affects text subtitles.
#sub-speed=24/23.976                          # Multiplies subtitle event timestamps by the given value. Can be used for basic subtitle fps adjustments. Note: Changing the --sub-fps value can lead to wrong durations.
sub-fix-timing=yes                            # Adjusts subtitle timing to remove small gaps or overlaps between subtitles (if the difference is less than 210 ms, the gap or overlap is removed). In some cases, this may make things worse, so you can try disabling this option. Default is disabled.
sub-stretch-durations=yes                     # Extends the duration of subtitles so that they end when the next subtitle starts. This may help with subtitles that erroneously have zero duration, but it can also break normal subtitles. Default is disabled.
#sub-fps                                      # Specifies the frame rate of the subtitle file (Default: video fps). Only affects text subtitles.
#sub-speed=24/23.976                          # Multiplies subtitle event timestamps by the given value. Can be used to adjust the playback speed for frame-based subtitle formats. Only affects text subtitles.
                                              # --sub-speed=24/23.976: Play frame-based subtitles, assuming a frame rate of 23.976, 24 frames per second.
#stretch-dvd-subs=yes                         # Stretches DVD subtitles when playing anamorphic videos. Default is disabled.
#sub-filter-regex-plain=yes                   # Whether to first convert the ASS "text" field to plain text (Default: no). This strips ASS tags and applies ASS directives like \N to line breaks. If the result is multiline, regex anchors ^ and $ match each line, but any matches still discard all lines.
#sub-filter-regex-warn=yes                    # Logs discarded lines with a warning log level instead of detailed (Default: no). Helps with testing.
#sub-filter-regex-enable=no                   # Whether to enable regex filtering (Default: yes). The following two parameters are ineffective when empty.
#sub-filter-regex-append=opensubtitles\.org   # --sub-filter-regex-...=... Sets a list of regular expressions to match against text subtitles and remove any matching lines (Default: empty). Matching is case-insensitive but depends on libc and is likely only for ASCII. It doesn't apply to bitmap/image subtitles.
#sub-filter-jsre-append=                      # Same as --sub-filter-regex, but using JavaScript regex. Shares and is controlled by all --sub-filter-regex-* options, only requires JavaScript support.

##⇘⇘Subtitle settings, when no built-in styles are used.
#sub-fonts-dir=~~/fonts                       # Specifies the directory to search for fonts for text subtitles. The example is the default value.
sub-font="Noto Sans CJK SC"                   # Specifies the font for plain text subtitles. This parameter should be ineffective for ASS subtitles but actually affects the default font in ASS. https://github.com/mpv-player/mpv/issues/8637
sub-font-size=50                              # Font size for text subtitles.
sub-bold=yes                                  # Use bold style for text subtitles.
#sub-italic=yes                               # Use italic style for text subtitles.
sub-color="#FFFFFFFF"                         # Font color for text subtitles. <Format is (AA)RRGGBB> AA is the hexadecimal transparency, RRGGBB is the hexadecimal color representation.
#sub-back-color="#FF262626"                   # Background color for text subtitles.
sub-border-size=0.5                           # Border size for text subtitles.
sub-border-color="#FF000000"                  # Border color for text subtitles.
sub-shadow-offset=0.5                         # Shadow size for text subtitles.
sub-shadow-color="#FF000000"                  # Shadow/background color for text subtitles.
#sub-border-style=opaque-box                  # <Default outline-and-shadow|opaque-box|background-box> Style of text subtitle border.
                                              # outline-and-shadow: Draws outline and shadow; opaque-box: Draws outline and shadow as an opaque box tightly wrapping each line of text; background-box: Draws a background box wrapping all lines of text.
#sub-spacing=1                                # Letter spacing for text subtitles.
#sub-blur=0.5                                 # Edge blur for text subtitles. Recommended values are between 0.5 and 3.0. <0..20.0>
#sub-use-margins=no                           # Whether to output plain text subtitles on the black border. Default is yes.
#sub-pos=100                                  # Specifies the position of subtitles on the screen. This value is the vertical position of the subtitles, as a percentage of the screen height. Default is 100, values above 100 move the subtitles down. Affects both ASS and plain text subtitles.
#sub-margin-x=25                              # Left and right screen margins for subtitles in scaled pixels. This option specifies the distance from the left side and the distance from the right side of the screen for plain text subtitles.
#sub-margin-y=22                              # Top and bottom screen margins for subtitles in scaled pixels. This option specifies the vertical margins for plain text subtitles. If you only want to increase the vertical subtitle position, use --sub-pos.
#sub-align-x=center                           # <left|Default center|right> Controls which corner of the screen the text subtitles should align to. Not applied to ASS subtitles unless in --no-sub-ass mode. This also does not apply to PGS image subtitles.
#sub-align-y=bottom                           # <top|center|Default bottom> Controls the vertical position of text subtitles.
#sub-justify=left                             # <Default auto|left|center|right> Controls alignment for multi-line subtitles (Default: auto, aligns according to --sub-align-y). Left alignment is recommended.
#sub-ass-justify=yes                          # If --sub-ass-override is not set to no, applies --sub-justify to ASS subtitles. Default is no.



########
# ScreenShot #
########

### The following preset parameters are for capturing the highest quality images
screenshot-format=webp                        # <Default jpg|(same as before)jpeg|png|webp|jxl|avif>
#screenshot-png-compression=7                 # <0-9> PNG compression level. Higher levels affect performance; the default is 7
#screenshot-png-filter=5                      # Sets the filter applied before PNG compression. 0 is none, 1 is sub, 2 is up, 3 is average, 4 is Paeth, 5 is mixed. This affects the achievable compression level. For most images, mixed provides the best compression ratio, so it is the default setting.
#screenshot-jpeg-quality=100                  # <0-100> Maximum JPEG quality; the default is 90
#screenshot-jpeg-source-chroma=no             # Write JPEG with the same chroma subsampling as the source video; the default is yes
#screenshot-webp-lossless=yes                 # Lossless WEBP; the default is no
screenshot-webp-quality=85                    # <0-100> [Effective when --screenshot-webp-lossless=no] Quality of lossy WEBP; the default is 75
screenshot-webp-compression=6                 # <0-6> WEBP compression level. Higher levels affect performance; the default is 4. Note: this also affects image quality when using lossy WEBP.
#screenshot-jxl-distance=0                    # <0-15> JXL visual model distance; 0 is lossless, 0.1 is visually lossless, and the default 1 is equivalent to JPEG quality of 90
#screenshot-jxl-effort=5                      # <1-9> JXL compression level. Higher levels affect performance; the default is 4
screenshot-tag-colorspace=no                  # Use appropriate colorspace tags for screenshots (not all formats are supported); the default is yes
#screenshot-high-bit-depth=no                 # Primarily affects PNG and JPEG XL. Use the same bit depth as video output when possible; the default is yes. Note: using the same bit depth significantly increases the final size of png/jxl.
#screenshot-sw=yes                            # Whether to use software rendering for screenshots; the default is no. If set to yes, a software scaler is used to convert video to RGB (or whatever the target screenshot requires). The software renderer may lack certain features, such as HDR rendering.
screenshot-template="~~/files/screen/%{media-title}-%P-%n"
                                              # [If the path is set directly in the template, there is no need for --screenshot-dir] Screenshot naming template: https://mpv.io/manual/master/#options-screenshot-template
#screenshot-dir=~~desktop/                    # [If the screenshot path is set in the template, there is no need to use this option] Default save location for screenshots is the desktop


####################
# 脚本 滤镜 着色器 #
####################

gpu-shader-cache-dir="~~/cache/shaders_cache"                                   # 指定目录存储编译好的 GPU shaders，加速启动。默认启用
                                                                                ## 注意！如果不放 APPDATA 下，确保该文件夹有用户写入权限。
                                                                                ## 缓存生成的着色器本应起到加速作用，但是硬盘 IO 性能不足可能产生反作用，根据实际情况选择禁不禁用
##⇘⇘脚本部分
##内置脚本开关（如果没有必要的目的，那就不要屏蔽 mpv 内建的功能）
#load-scripts=no                                                                # 自动加载配置文件同目录下的 scripts 子目录中的所有外置脚本，默认 yes。设置为 no 时可用下一行示例的命令加载指定的外置脚本，不推荐
#scripts="~~/scripts/autoload.lua;~~/scripts/playlistmanager.lua;~~/scripts/open_dialog.lua"
#load-auto-profiles=no                                                          # <yes|no|默认 auto> 启用 mpv 内置的条件配置脚本 [auto_profiles.lua]
#load-osd-console=no                                                            # 启用 mpv 内置的控制台脚本 [console.lua]，默认 yes
#load-stats-overlay=no                                                          # 启用 mpv 内置的统计信息脚本 [stats.lua]，默认 yes
#ytdl=no                                                                        # 启用 mpv 内置的网址解析增强脚本 [ytdl-hook.lua]，默认 yes
#ytdl-format=bestvideo+bestaudio/best                                           # <ytdl|best|worst|mp4|webm|...> 直接传递给 youtube-dl 的视频格式/质量。默认值：bestvideo+bestaudio/best
                                                                                ## ytdl 不会将 --format 选项传递给 youtube-dl，因此不会覆盖其默认值。请注意，有时 youtube-dl 会返回 mpv 无法使用的格式，在这些情况下，mpv 默认值可能会更好
#ytdl-raw-options=sub-langs="zh.*",write-subs=,write-auto-subs=
#ytdl-raw-options-append=write-subs=
#ytdl-raw-options-append=yes-playlist=
#ytdl-raw-options-append=cookies-from-browser=Firefox                           # 传递并使用浏览器的 cookies，支持 Firefox、Chrome、Chromium、Edge、Opera、Vivaldi 等浏览器
                                                                                ## 注意：cookies-from-browser 选项需要浏览器已经登录目标网站，且 cookies 未过期
                                                                                ## Chromium 系浏览器（Chrome、Edge 等）需要关闭所有进程才能读取 cookies，或者使用 --ytdl-raw-options-append=cookies=cookies.txt
#ytdl-raw-options-append=cookies=[D:/cookies.txt]                               # 传递并使用 cookies.txt 文件，只支持绝对路径
#ytdl-raw-options-append=proxy=http://127.0.0.1:7890
                                                                                # [当使用 yt-dlp/youtube-dl 解析网址时有效] --ytdl-raw-options=<key>=<value>[,<key>=<value>[,...]] 将自定义的选项传递给 ytdl https://github.com/yt-dlp/yt-dlp#usage-and-options
                                                                                ## 传递多个参数最好独立追加，即写多个 --ytdl-raw-options-append 参数 https://mpv.io/manual/master/#options-ytdl-raw-options
#stream-lavf-o-append=http_proxy=http://127.0.0.1:7890                          # [当使用 libavformat 解析网址时有效] --stream-lavf-o-append=<key>=<value>[,<key>=<value>[,...]] 将自定义的选项传递给 libavformat
                                                                                ## 传递多个参数最好独立追加，即写多个 --stream-lavf-o-append 参数 https://mpv.io/manual/master/#options-stream-lavf-o
                                                                                ## 注意，实际上此选项设置的代理也会传递给 ytdl 以及其他网络请求
#input-builtin-bindings=no                                                      # 在启动期间禁用 mpv 内置键位绑定的加载。此选项不会影响外部加载脚本的键位绑定
input-default-bindings=no                                                       # 禁用 mpv 内置键位及外部加载脚本的--mp.add_key_binding 键位绑定方案
                                                                                ## 可以有效解决脚本快捷键冲突，启用后需在 input.conf 里指定所需快捷键

##⇘⇘滤镜部分
## 设定随 MPV 启动的音/视频滤镜的书写格式（支持多项） --af/vf=滤镜①=参数❶=值:参数❷=值,滤镜②...
## --af 和 --vf 仅能各存在一条。如果不想只使用这条参数，可以拆开写，例如使用 --vf-toggle 单项多次追加更多的滤镜，并不会覆盖 --vf=<value> 指定的滤镜
## 同类别的滤镜算法互斥

## 多通道音轨调节各通道音，防止背景音或人声过小（双通道设备）
#af-toggle=@dynaudnorm:lavfi=[dynaudnorm=f=500:g=31:p=0.5:m=5:r=0.9]
#af-toggle=@dynaudnorm:!dynaudnorm=p=0.65:r=0.9
## 另一组示例
#af-toggle=@loudnorm:lavfi=[loudnorm=I=-16:TP=-1.5:LRA=11]
#af-toggle=@loudnorm:!loudnorm=I=-25:TP=-1.5:LRA=4:linear=false

## 音频变速滤镜三选一（当前默认值已足够好）
#af=scaletempo=scale=1.0:stride=60:overlap=.20:search=14:speed=tempo
                                                                                # 旧版 mpv 使用的音频变速滤镜。示例值为默认参数
                                                                                # 其中的 scale=<N> 控制额外视频加速度；stride=<N> 值太高会导致在高速时跳音，低速出现回声，值太低会改变音调，值增大提升性能
                                                                                # overlap=<N> 重叠百分比，值减少提高分离度且提升性能；search=<N> 搜索最佳重叠位置的长度（毫秒），值减少提升性能
                                                                                # speed=<tempo|pitch|both|none> tempo 节奏与速度同步，pitch 造成卡顿不使用，both 同时兼顾节奏与音调，none 忽略速度变化不使用
#af=scaletempo2=min-speed=0.25:max-speed=4.0:search-interval=30:window-size=20  
                                                                                # 此项参数为变速播放时默认自动激活的音频滤镜。在 min/max-speed=<N> 此速度范围外直接静音处理。scaletempo2 具有更高的音频质量
#af=rubberband                                                                  # 声音质量介于 scaletempo 和 scaletempo2 之间。可调节的细参众多，详情见官方文档，这里直接使用默认参数 

#vf-toggle=format:gamma=gamma2.2                                                # MPV 校色曲线目标是 BT.1886，此视频滤镜用于回归常见的显示器标准 https://github.com/mpv-player/mpv/issues/8009
#vf-toggle=fps=fps=60/1.001                                                     # 强制视频以指定帧率输出（此项与补帧冲突），通常被用于增强滚动弹幕的平滑性，不推荐使用 --sub-fps=<value> 。分子数值（取整）为你的显示器刷新率。

##⇘⇘着色器部分
### 着色器相关设置使用配置组进行设定，以下仅为参数示例说明，具体设置见下方 [通用参数补充区] 和 [配置组] 的相关配置组参数
## 此处的 --glsl-shaders=<value> 用于指定每次随 MPV 共同启动的着色器（支持多项）
## 初始加载多个着色器的示例写法
#glsl-shaders="~~/shaders/igv/KrigBilateral.glsl;~~/shaders/igv/FSRCNNX_x2_8-0-4-1.glsl;~~/shaders/igv/FSRCNNX_x2_8-0-4-1.glsl;~~/shaders/igv/SSimSuperRes.glsl;~~/shaders/igv/SSimDownscaler.glsl"
#glsl-shaders="~~/shaders/Anime4K/glsl/Restore/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/glsl/Restore/Anime4K_Restore_CNN_Soft_M.glsl;~~/shaders/Anime4K/glsl/Upscale/Anime4K_Upscale_CNN_x2_S.glsl"

## --glsl-shaders-append 等效 --glsl-shader=<value> （注意和上行说明中参数的区别），表示追加着色器（单次仅能追加一项），并不会覆盖第一条 --glsl-shaders=<value> 指定的着色器，可无限追加该命令。
#glsl-shaders-append="~~/shaders/other/PixelClipper.glsl"                       # 抗振铃着色器，主要用于 mpv 内部 EWA 类缩放算法。与--vo=gpu-next 内部的 AR 算法相比表现要稍好一些
#glsl-shaders-append="~~/shaders/other/JointBilateral.glsl"                     # 双边色度升频着色器 - 高斯版本，使用亮度平面作为指导，在不引入任何振铃的情况下实现更清晰的边缘。仅限--vo=gpu-next 下可用
#glsl-shaders-append="~~/shaders/other/nlmeans.glsl"                            # 降噪着色器，效果表现良好，只是性能耗费较高，仅限--vo=gpu-next 下可用。放置优先级应保持在最前列，否则放大后再降噪的效果会有明显下降
#glsl-shaders-append="~~/shaders/other/colorlevels.glsl"                        # 调整 RGB 输出范围的着色器。如需使用需按设备实际情况调整该着色器里的 REFBLACK 和 REFWHITE 值，否则输出画面可能不符合预期行为
#glsl-shaders-append="~~/shaders/igv/KrigBilateral.glsl"                        # 高级的 cscale 色度升频算法，生效时将覆盖指定的--cscale=xxxxx 算法。目前最优秀的色度升频着色器
#glsl-shaders-append="~~/shaders/igv/FSRCNNX_x2_8-0-4-1.glsl"                   # 快速超分辨率卷积神经网络放大算法。最小缩放触发倍率为 1.2，生效时将覆盖指定的--scale=xxxxx 算法
#glsl-shaders-append="~~/shaders/igv/FSRCNNX_x2_8-0-4-1_LineArt.glsl"           # 快速超分辨率卷积神经网络放大算法（anime）。最小缩放触发倍率为 1.2，生效时将覆盖指定的--scale=xxxxx 算法
#glsl-shaders-append="~~/shaders/igv/FSRCNNX_x2_16-0-4-1.glsl"                  # 快速超分辨率卷积神经网络放大算法。16x 效果更好，耗能也更高。最小缩放触发倍率为 1.2，生效时将覆盖指定的--scale=xxxxx 算法
#glsl-shaders-append="~~/shaders/igv/FSRCNNX_x2_16-0-4-1_enhance.glsl"          # 快速超分辨率卷积神经网络放大算法。去伪影变体，最小缩放触发倍率为 1.2，生效时将覆盖指定的--scale=xxxxx 算法
#glsl-shaders-append="~~/shaders/igv/FSRCNNX_x2_16-0-4-1_anime_enhance.glsl"    # 快速超分辨率卷积神经网络放大算法。anime 去伪影变体，最小缩放触发倍率为 1.2，生效时将覆盖指定的--scale=xxxxx 算法
#glsl-shaders-append="~~/shaders/nnedi3/nnedi3-nns32-win8x4.glsl"               # 超高质量的插值放大算法。最小缩放触发倍率为 1.2，耗能极高，nns16 → nns32 → nns64 → nns128 → nns256；win8x4 → win8x6 质量更好但性能大增
#glsl-shaders-append="~~/shaders/ravu/ravu-zoom-ar-r3.glsl"                     # ravu 放大算法 zoom 变体，通用型，触发倍率＞1，放大到目标分辨率，生效时将覆盖指定的--scale=xxxxx 算法
#glsl-shaders-append="~~/shaders/ravu/ravu-lite-ar-r3.glsl"                     # ravu 放大算法 lite 变体，通用型，最小缩放触发倍率约为 1.2，最锐利的变体，生效时将覆盖指定的--scale=xxxxx 算法
#glsl-shaders-append="~~/shaders/Anime4K/glsl/Restore/Anime4K_Restore_CNN_Soft_M.glsl" 
                                                                                # Anime4k 放大算法模块（适度画面修复 + 弱感知强化 + 微量伪影引入），CNN 变体最小缩放触发倍率为 1.2，推荐 Restore_CNN_Soft 抗锯齿柔化变体，减少了振铃等其他伪影的产生
                                                                                # Anime4k 着色器介绍及用法参考：https://hooke007.github.io/mpv-lazy/[01]_%E7%AC%AC%E4%B8%89%E6%96%B9%E7%9D%80%E8%89%B2%E5%99%A8%E4%BB%8B%E7%BB%8D.html
#glsl-shaders-append="~~/shaders/igv/SSimSuperRes.glsl"                         # 对 mpv 的内置 --scale=xxxxx 放大算法进行校正（消除振铃伪影，恢复原始清晰度等）
#glsl-shaders-append="~~/shaders/igv/SSimDownscaler.glsl"                       # 缩小算法增强，与--linear-downscaling=no 一起使用
#glsl-shaders-append="~~/shaders/igv/adaptive-sharpen.glsl"                     # 自适应锐化算法着色器


##################
# 通用参数补充区 #
##################

#d3d11-output-csp=pq                          # [须 --gpu-api=d3d11] 此项参数用于直出 HDR，且不支持条件配置会影响 sdr 播放。
                                              # 当前版本需先手动打开 win10 OS HDR，再打开 MPV 后全屏开始播放
#use-filedir-conf                             # 在播放文件同目录中加载配置文件
#include="~~/profiles.conf"                   # 追加读取指定的配置文件
                                              # 自此以下的所有配置组参数亦可单独放入"~~/profiles.conf"中，启用此参数时效果一致
#input-conf="~~/input.conf"                   # 指定 input 配置文件，用于指定键位绑定
libplacebo-opts=tone_map_metadata=cie_y
                                              # 将额外的原始选项传递给 libplacebo 渲染后端（由 --vo=gpu-next 使用），用于调试目的
                                              # 详细参数见：http://libplacebo.org/options
                                              # 示例参数作用为：强制使用峰值检测进行 hdr 色调映射

##⇘⇘以下为常规配置组启用参数，视需求选择使用
#profile=ICC                                  # icc 色彩管理配置组，由操作系统的显示设置指定的 ICC 显示配置文件
#profile=ICC+                                 # icc 色彩管理配置组，使用 color.org 提供的泛 icc 配置文件或专业校色文件，自行选择显示器对应色域的 icc 配置文件
profile=Target                                # target 色彩管理配置组，不可与 icc 色彩管理共存
profile=Dither                                # 抖动算法配置组（fruit），效果接近 error-diffusion，耗能较低
#profile=Dither+                              # 抖动算法配置组（error-diffusion），误差扩散的色深抖动算法，效果最好也最耗能
profile=Tscale                                # [启用前须先注释 --tscale=<value>] 时域插值算法，具体参数见下方配置区的同名配置
#profile=Tscale+                              # [启用前须先注释 --tscale=<value>] 备选的时域插值算法，具体参数见下方配置区的同名配置
profile=HQ                                    # 最常用的内置算法配置组
profile=DeBand-low                            # [启用前需先注释 --deband] 预设的去色带方案，具体参数见下方配置区的同名配置，接近 madvr 的 deband 设为 low，使用 low 时已有很好的效果
#profile=DeBand-medium                        # [启用前需先注释 --deband] 备选的去色带方案，具体参数见下方配置区的同名配置，接近 madvr 的 deband 设为 medium
#profile=DeBand-high                          # [启用前需先注释 --deband] 备选的去色带方案，具体参数见下方配置区的同名配置，接近 madvr 的 deband 设为 high
#profile=HDR-PASS                             # HDR 直通参数配置组。HDR 直通时需禁用--icc-profile,icc-profile-auto 和--target-trc 参数。--vo=gpu-next --gpu-api=vulkan 下和全屏（Windows 的限制）时可用，--gpu-api=d3d11 下需手动切换显示器 HDR 模式（Windows 的限制）
#profile=SDR2HDR                              # SDR 映射 HDR 参数配置组，仅在 hdr 设备上有效，不推荐启用。仅限--vo=gpu-next 下可用
profile=HDR2SDR                               # HDR 映射参数配置组
#profile=SWscaler                             # 备选的软件缩放方案

## 以下为着色器配置组，可按需启用以下所需任一配置组（这将覆盖上面的--glsl-shaders-append 参数）
profile=NNEDI3                                # 适用于大多数场景（NNEDI3-32）               ## 绝大多数场景表现良好，对场景的适应性优于其他配置（抑制锯齿和振铃伪影）。喜欢通用性的优先使用此配置组
#profile=NNEDI3+                              # 适用于大多数场景（NNEDI3-64）               ## 理论上 NNEDI3-64 及以上变体具有更好地抑制振铃伪影效果，但耗能量级倍增，视设备性能自行选择是否使用
#profile=AMD-FSR_EASU                         # 适用于大多数场景（AMD-FSR_EASU）            ## AMD 的 FSR 算法中 EASU 变体表现良好，相对于 FSR 和 RCAS 变体产生更少的振铃伪影，同时有不错的画面效果。放大至目标分辨率
#profile=ravu-zoom                            # 适用于大多数场景（ravu-zoom）               ## 放大到目标分辨率
#profile=FSRCNNX                              # 适用于 HD 场景（FSRCNNX）                   ## 效果类似 madvr 的 NGU Sharp，但产生更多振铃伪影，在低解析画面下表现较差
#profile=FSRCNNX+                             # 适用于 SD 场景（FSRCNNX_enhance）           ## 对低解析度的实拍类图像具有极好的抑制振铃伪影效果
#profile=FSRCNNX-anime                        # 适用于 HD 动画（FSRCNNX-anime）             ## 效果类似 madvr 的 NGU AA，但产生更多振铃伪影，在低解析画面下表现较差
#profile=FSRCNNX-anime+                       # 适用于 SD 动画（FSRCNNX-anime_enhance）     ## 对低解析度的 anime 类图像具有极好的抑制振铃伪影效果
#profile=Anime4K                              # 适用于大多数动画（Anime4k 去伪影抗锯齿）    ## 对锯齿振铃等瑕疵可以起到较好的抑制效果，仍有明显感知度的涂抹劣化
#profile=Anime4K+                             # 适用于 SD 动画（Anime4k 高强度抗锯齿）      ## 对 anime 类图像具有更好的抑制锯齿振铃伪影等瑕疵的效果，强度更高。UL 变体实际效果最好，但耗能极高，暂不推荐使用
#profile=SSIM                                 # 适用于低性能消耗

##############
# 常规配置组 #
##############

##⇘⇘常规配置没有自动触发条件（内置配置组也全为常规配置）
##需要主设置文件中使用 --profile=<profile1,profile2,...> 或多行 --profile=<xxx> 参数激活数个配置

[ICC]
 icc-profile=""
 icc-profile-auto
 icc-intent=0
 icc-force-contrast=1000
 icc-3dlut-size=auto
 icc-cache-dir="~~/cache/icc_cache"

[ICC+]
 icc-profile="~~/icc/ITU-RBT709ReferenceDisplay.icc"
 icc-intent=0
 icc-force-contrast=no
 icc-3dlut-size=auto
 icc-cache-dir="~~/cache/icc_cache"

[Target]
 icc-profile=""
 icc-profile-auto=no
 target-prim=auto
 target-trc=gamma2.2
 target-contrast=auto
 target-peak=auto

[Dither]
 dither-depth=auto
 dither=fruit
 dither-size-fruit=6

[Dither+]
 dither-depth=auto
 dither=error-diffusion
 error-diffusion=floyd-steinberg

[Tscale]
 video-sync=display-resample
 #video-sync-max-video-change=5
 interpolation
 #interpolation-preserve=no
 tscale=oversample

[Tscale+]
 video-sync=display-resample
 #video-sync-max-video-change=5
 interpolation
 #interpolation-preserve=no
 tscale=sphinx
 tscale-blur=0.65

[HQ]
 scale=ewa_hanning
 cscale=bilinear
 dscale=catmull_rom
 scale-antiring=0.5
 dscale-antiring=0.5
 linear-upscaling=no
 sigmoid-upscaling=yes
 correct-downscaling=yes
 linear-downscaling=no
 #scaler-resizes-only=no
 deband=no

[DeBand-low]
 profile-desc=DeBand low
 deband-iterations=1
 deband-threshold=48
 deband-range=16
 deband-grain=16

[DeBand-medium]
 profile-desc=deband medium
 deband-iterations=2
 deband-threshold=64
 deband-range=16
 deband-grain=24

[DeBand-high]
 profile-desc=deband high
 deband-iterations=3
 deband-threshold=64
 deband-range=16
 deband-grain=24

[HDR-PASS]
 profile-desc=HDR 直通参数
 target-colorspace-hint=yes

[SDR2HDR]
 profile-desc=SDR 反向映射 HDR 参数
 inverse-tone-mapping=yes

[HDR2SDR]
 profile-desc=HDR-SDR 映射参数
 tone-mapping=auto
 #tone-mapping-param=1.0
 #tone-mapping-max-boost=1.0
 gamut-mapping-mode=auto
 hdr-contrast-recovery=0.30
 #hdr-contrast-smoothness=3.5
 hdr-compute-peak=auto
 hdr-peak-percentile=99.995
 hdr-peak-decay-rate=20
 hdr-scene-threshold-low=1.0
 hdr-scene-threshold-high=3.0
 allow-delayed-peak-detect=no

##⇘⇘备选的软件缩放方案
##在 mpv 内部需要执行格式转换时会用到软件缩放器
[SWscaler]
 profile-desc=软件缩放配置
 sws-allow-zimg=no                  # 优先使用 zimg（通常具有更高质量）而不是 libswscale，默认 yes。ffmpeg 经常出现和 zimg 间的兼容性问题，建议禁用
 sws-scaler=bicublin                # [libswscale] <fast-bilinear|bilinear|bicubic 默认|x|point|area|bicublin|gauss|sinc|lanczos|spline>
                                    # [libswscale] 缩放算法（同时作用于色度平面）
 #sws-fast=yes                      # [libswscale] 以质量为代价优化性能，默认 no
 zimg-scaler-chroma=bicubic         # [zimg] <point|bilinear|bicubic|spline16|spline36|lanczos 默认>
                                    # [zimg] 色度还原算法
 #zimg-scaler-chroma-param-a=1/3    # [zimg] 色度还原算法的子参数，不支持调节的算法不受影响，默认 default
 #zimg-scaler-chroma-param-b=1/3
 zimg-scaler=bicubic                # [zimg] 缩放算法，可用值参考 --zimg-scaler-chroma
 zimg-scaler-param-a=1/3            # [zimg] 缩放算法的子参数
 zimg-scaler-param-b=1/3
 #zimg-dither=error-diffusion       # [zimg] <no|ordered|random 默认|error-diffusion> 色深抖动算法
 zimg-fast=no                       # [zimg] 以质量为代价优化性能，默认 yes


[NNEDI3]
 profile-desc=适用于大多数场景
 glsl-shaders="~~/shaders/nnedi3/nnedi3-nns32-win8x4.glsl;~~/shaders/igv/KrigBilateral.glsl;~~/shaders/igv/SSimSuperRes.glsl;~~/shaders/igv/SSimDownscaler.glsl"

[NNEDI3+]
 profile-desc=适用于大多数场景
 glsl-shaders="~~/shaders/nnedi3/nnedi3-nns64-win8x4.glsl;~~/shaders/igv/KrigBilateral.glsl;~~/shaders/igv/SSimSuperRes.glsl;~~/shaders/igv/SSimDownscaler.glsl"

[AMD-FSR_EASU]
 profile-desc=适用于大多数场景
 glsl-shaders="~~/shaders/igv/KrigBilateral.glsl;~~/shaders/AMD-FSR/AMD-FSR-EASU_rgb.glsl;~~/shaders/igv/SSimDownscaler.glsl"

[ravu-zoom]
 profile-desc=适用于大多数场景
 glsl-shaders="~~/shaders/ravu/ravu-zoom-ar-r3.glsl;~~/shaders/igv/KrigBilateral.glsl;~~/shaders/igv/SSimDownscaler.glsl"

[FSRCNNX]
 profile-desc=适用于 HD 场景
 glsl-shaders="~~/shaders/igv/FSRCNNX_x2_8-0-4-1.glsl;~~/shaders/igv/KrigBilateral.glsl;~~/shaders/igv/SSimSuperRes.glsl;~~/shaders/igv/SSimDownscaler.glsl"

[FSRCNNX+]
 profile-desc=适用于 SD 场景
 glsl-shaders="~~/shaders/igv/FSRCNNX_x2_16-0-4-1_enhance.glsl;~~/shaders/igv/KrigBilateral.glsl;~~/shaders/igv/SSimSuperRes.glsl;~~/shaders/igv/SSimDownscaler.glsl"

[FSRCNNX-anime]
 profile-desc=适用于 HD 动画
 glsl-shaders="~~/shaders/igv/FSRCNNX_x2_8-0-4-1_LineArt.glsl;~~/shaders/igv/KrigBilateral.glsl;~~/shaders/igv/SSimSuperRes.glsl;~~/shaders/igv/SSimDownscaler.glsl"

[FSRCNNX-anime+]
 profile-desc=适用于 SD 动画
 glsl-shaders="~~/shaders/igv/FSRCNNX_x2_16-0-4-1_anime_enhance.glsl;~~/shaders/igv/KrigBilateral.glsl;~~/shaders/igv/SSimSuperRes.glsl;~~/shaders/igv/SSimDownscaler.glsl"

[Anime4K]
 profile-desc=Anime4K
 profile-desc=适用于大多数动画
 glsl-shaders="~~/shaders/igv/KrigBilateral.glsl;~~/shaders/Anime4K/glsl/Restore/Anime4K_Restore_CNN_Soft_M.glsl;~~/shaders/Anime4K/glsl/Restore/Anime4K_Clamp_Highlights.glsl"

[Anime4K+]
 profile-desc=Anime4K+
 profile-desc=适用于 SD 动画
 glsl-shaders="~~/shaders/igv/KrigBilateral.glsl;~~/shaders/Anime4K/glsl/Restore/Anime4K_Restore_CNN_Soft_VL.glsl;~~/shaders/Anime4K/glsl/Restore/Anime4K_Clamp_Highlights.glsl"

[SSIM]
 profile-desc=适用于 4k 低性能
 glsl-shaders="~~/shaders/other/FastBilateral.glsl;~~/shaders/igv/SSimSuperRes.glsl;~~/shaders/igv/SSimDownscaler.glsl"

##############
# 条件配置组 #
##############

##⇘⇘条件触发配置，根据给定条件激发特定参数。简易格式见示例
##仅注释掉 --profile-cond 和 --profile-restore 即可将条件配置变为常规配置，常规配置的使用方法为通用区 --profile=<value>

##⇘⇘条件配置的格式示例
##[FS-loop]                                                  # 配置名称，自定义
##profile-desc    = 全屏时循环播放当前文件                   # 配置的描述，不属于参数，随意写
##profile-cond    = fullscreen                               # 触发该配置的条件。可用的属性列表 https://mpv.io/manual/master/#property-list
##profile-restore = copy                                     # 此参数一般用于回归触发前的通用参数（关闭此项将保留）
##...                                                        # 然后写该条件下触发的参数
##...

##⇘⇘文件名匹配的示例写法：根据文件名中的特定字符触发配置
##字符 "-" 需要使用 LUA 的转义符 "%"
#[VCB-Studio]
#profile-desc=真空断路器
#profile-cond=filename:find("VCB%-Studio")~=nil
#profile-restore=copy
#deband=no

#[DVD-Anime]
#profile-desc=2D 低清源增强
#profile-cond=height<=576
#profile-restore=copy
#glsl-shaders="~~/shaders/other/FastBilateral.glsl;~~/shaders/igv/FSRCNNX_x2_8-0-4-1_LineArt.glsl;~~/shaders/igv/FSRCNNX_x2_8-0-4-1_LineArt.glsl;~~/shaders/igv/SSimSuperRes.glsl;~~/shaders/igv/SSimDownscaler.glsl"

[Deband]
 profile-desc=YUV420P10 以下的源开启去色带
 profile-cond=get("video-params/average-bpp") < 24
 profile-restore=copy
 deband=yes

[hdr-2390]
 profile-cond=p.tone_mapping == "bt.2390" and p.current_vo == "gpu-next"
 profile-restore=copy
 tone-mapping-param=2.0

#[hdr-spline]
#profile-cond=p.tone_mapping == "spline"
#profile-restore=copy
#tone-mapping-param=1.0

#[DoVi-P5]
#profile-desc=DoVi-P5 转换 HDR 直通
#profile-cond=get("video-params/colormatrix") == "dolbyvision" and get("video-params/colorlevels") == "full"
#profile-restore=copy
#target-trc=bt.2020
#target-gamut=dci-p3
#target-trc=pq

[HDR]
 profile-desc=HDR 配置
 profile-cond=get("video-out-params/max-luma") > 203
 profile-restore=copy
 #icc-profile=""
 #icc-profile-auto=no
 #target-trc=auto
 #target-peak=100
 blend-subtitles=no
 deband=no
 vf=""

[SDR-Gamut]
 profile-desc=SDR 内容的色域映射
 profile-cond=get("video-params/primaries") ~= "bt.709" and get("video-params/gamma") == "bt.1886"
 profile-restore=copy
 gamut-mapping-mode=clip

[video-sync]
 profile-desc=当音频调整速度超过阈值时改变音视频同步模式，以避免明显的音高变化
 profile-cond=(speed * audio_speed_correction) > 0 and ((speed * audio_speed_correction) < 0.96 or (speed * audio_speed_correction) > 1.04) and not (get("estimated-vf-fps") > 47 or get("display-fps") > 120)
 profile-restore=copy-equal
 video-sync=display-tempo

[fps-fix]
 profile-desc=修复视频帧率和显示刷新率过高引起的异常耗能或掉帧
 profile-cond=get("estimated-vf-fps") > 47 or get("display-fps") > 120
 profile-restore=copy
 video-sync=audio

[8k-fix]
 profile-desc=8k av1 下使用 d3d11va 硬解来解决掉帧问题，该方案仅限 --gpu-api=d3d11 下可用
 profile-cond=get("video-params/w") > 7000 or get("video-params/h") > 3000
 profile-restore=copy
 hwdec=auto-safe
 video-sync=audio

[pgs-fix]
 profile-desc=修复国内电影压制的内封 pgs 字幕脚本比例与视频不一致导致的字幕错位
 profile-cond=get("video-params/aspect") > 16 / 9
 profile-restore=copy
 stretch-image-subs-to-screen=yes

#[deinterlace]
# profile-desc=Automatic deinterlacing
# profile-cond=get("video-frame-info/interlaced")
# profile-restore=copy
# deinterlace

#[audio-filter]
#Side effect: Causes continuous A/V desynchronization when switching Bluetooth headphones
# profile-desc=Enable dynamic range compression filter when audio channels exceed 2, suitable for dual-channel devices
# profile-cond=get("audio-params/channel-count") > 2
# profile-restore=copy-equal
# af-pre=@dynaudnorm:lavfi=[dynaudnorm=f=500:g=31:p=0.5:m=5:r=0.9]

[pause]
 profile-desc=Disable always-on-top when paused
 profile-cond=pause
 profile-restore=copy
 ontop=no

#[fullscreen]
# profile-desc=Automatically set to always-on-top in fullscreen
# profile-cond=fullscreen or window_maximized
# profile-restore=copy
# ontop

[maximized]
 profile-desc=Disable auto-resize when maximized
 profile-cond=window_maximized and vid and not get("current-tracks/video/albumart")
 profile-restore=copy-equal
 auto-window-resize=no

[minimized]
 profile-desc=Pause when minimized
 profile-cond=window_minimized and vid and not get("current-tracks/video/albumart")
 profile-restore=copy-equal
 pause

[end]
 profile-desc=Exit fullscreen after playlist ends
 profile-cond=idle_active
 no-fullscreen
 no-window-maximized

[media-title]
 profile-desc=Switch window title display content under special protocols
 profile-cond=path:find('://') ~= nil or path:find('^magnet:') ~= nil
 #profile-cond=demuxer_via_network == true
 profile-restore=copy
 title=${?pause==yes:⏸}${?mute==yes:🔇}${?ontop==yes:📌}${media-title}

[smaller-screen]
 profile-desc=4K 屏窗口模式下不要缩放 720p 视频
 profile-cond=display_height >= 2160
 profile-restore=copy
 autofit-smaller=30%x30%

#[image]
# profile-desc=Image
# profile-cond=vid and not get("current-tracks/video/albumart") and estimated_frame_count <= 1
# profile-restore=copy
# prefetch-playlist
# image-display-duration=inf
# scale=ewa_hanning
# cscale=ewa_hanning
# dscale=mitchell

#[audio]
# profile-desc=Music
# profile-cond=aid and (not vid or get("current-tracks/video/albumart"))
# profile-restore=copy
# prefetch-playlist
# loop-playlist=inf

 ##############
 # Legacy Configuration #
 ##############

##⇘⇘Auto-config for older versions, limited functionality, generally not used. Syntax in examples (can be converted to conditional profile configuration)

##⇘⇘Activate corresponding parameters based on file extensions during playback, deactivate if not met
##[extension.extension]
##Do not write --profile-cond and --profile-restore
##... # Directly write the parameters

#[extension.vpy]
#profile-desc=Support preview for "xxxx.vpy" files
#demuxer-lavf-format=vapoursynth

##⇘⇘Activate corresponding parameters based on protocol during playback, deactivate if not met
##[protocol.protocol_name]
##Do not write --profile-cond and --profile-restore
##... # Directly write the parameters

#[protocol.https]
#profile-desc=Switch display content under https protocol
#osd-playing-msg='${media-title} ${!chapters==0:Chapter:${chapter}/${chapters}} ${!playlist-count==1:List:${playlist-pos-1}/${playlist-count}}'
#script-opts-append=osc_lazy-title=${media-title} ${!chapters==0:Chapter:${chapter}/${chapters}} ${!playlist-count==1:List:${playlist-pos-1}/${playlist-count}}

##⇘⇘Achieve automatic parameter and configuration file switching based on display name for mpv window using display-name.lua script (Windows only), dependent on MultiMonitorTool.exe
#[TV]
#profile-cond=get("user-data/display_name") == 'VX2771-4K-HD'
#profile-restore=copy-equal
#dither-depth=10
#inverse-tone-mapping=yes
#target-colorspace-hint=yes
#script-opts-append=changerefresh-auto=yes
#script-opts-append=changerefresh-rates=23;24;25-50;29-59;30-60;50;59;60